#include "Account.h"

Account::Account(int accountNumber, const Customer& accountHolder, double balance) : accountNumber(accountNumber), accountHolder(accountHolder), balance(balance) {}

// Implement methods for deposits, withdrawals, transfers, etc.

// Example deposit method
// void Account::deposit(double amount) {
//     balance += amount;
// }

// Include getter methods for account information
